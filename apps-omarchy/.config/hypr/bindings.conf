# Application bindings
bindd = SUPER ALT, RETURN, Tmux, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)" tmux
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://chatgpt.com"
bindd = SUPER SHIFT ALT, A, Grok, exec, omarchy-launch-webapp "https://grok.com"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://app.hey.com/calendar/weeks/"
bindd = SUPER SHIFT, E, Email, exec, omarchy-launch-webapp "https://app.hey.com"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# macOS-like ALT aliases (ALT = CMD)
bindd = ALT, C, Copy (macOS), sendshortcut, CTRL, Insert,
bindd = ALT, V, Paste (macOS), sendshortcut, SHIFT, Insert,
bindd = ALT, X, Cut (macOS), sendshortcut, CTRL, X,
bindd = ALT, Z, Undo (macOS), sendshortcut, CTRL, Z,
bindd = ALT SHIFT, Z, Redo (macOS), sendshortcut, CTRL, SHIFT+Z,
bindd = ALT, A, Select all (macOS), sendshortcut, CTRL, A,
bindd = ALT, W, Close tab (macOS), sendshortcut, CTRL, W,
bindd = ALT, Q, Quit app (macOS), killactive,
bindd = ALT, F, Find (macOS), sendshortcut, CTRL, F,
bindd = ALT, S, Save (macOS), sendshortcut, CTRL, S,
bindd = ALT, T, New tab (macOS), sendshortcut, CTRL, T,
bindd = ALT, N, New window (macOS), sendshortcut, CTRL, N,
bindd = ALT, H, Hide/minimize window (macOS), movetoworkspacesilent, special:minimized
bindd = ALT, M, Minimize window (macOS), movetoworkspacesilent, special:minimized
bindd = ALT, GRAVE, Cycle windows (macOS), cyclenext,
bindd = ALT, L, Focus address bar (macOS), sendshortcut, CTRL, L,
bindd = ALT, R, Refresh (macOS), sendshortcut, CTRL, R,
bindd = ALT, P, Print (macOS), sendshortcut, CTRL, P,
bindd = ALT SHIFT, T, Reopen closed tab (macOS), sendshortcut, CTRL, SHIFT+T,

# macOS-like Mission Control (CTRL+arrows)
bindd = CTRL, UP, Mission Control (macOS), hyprtasking:toggle, all
bindd = CTRL, DOWN, Mission Control (macOS), hyprtasking:toggle, all
bindd = CTRL, LEFT, Previous space (macOS), workspace, e-1
bindd = CTRL, RIGHT, Next space (macOS), workspace, e+1

# macOS-like screenshot aliases (CMD+Shift+3/4/5)
bindd = ALT SHIFT, code:12, Screenshot fullscreen (macOS), exec, omarchy-cmd-screenshot fullscreen
bindd = ALT SHIFT, code:13, Screenshot selection (macOS), exec, omarchy-cmd-screenshot region
bindd = ALT SHIFT, code:14, Capture menu (macOS), exec, omarchy-menu capture

# Add extra bindings
# bind = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Disable SUPER+W from closing windows (was: killactive)
unbind = SUPER, W

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu

#bind = SUPER SHIFT, Z, hyprexpo:expo, toggle
#bind = SUPER, g, hyprtasking:toggle, all
bind = , mouse:275, hyprtasking:toggle, all
bind = SUPER, z, easymotion, action:hyprctl dispatch focuswindow address:{}
