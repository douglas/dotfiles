# Extra autostart processes
# exec-once = uwsm-app -- my-service

# Omarchy starts mako from its default autostart before Quickshell registers
# notifications from the shared personal profile.
exec-once = pkill -x mako || true
