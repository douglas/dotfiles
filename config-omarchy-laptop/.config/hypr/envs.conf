# Render on Intel Arc iGPU by default (power efficiency).
# Use nvidia-offload <app> for GPU-heavy apps (gaming, CUDA, etc.)
# or set these env vars per-app:
#   __NV_PRIME_RENDER_OFFLOAD=1 __GLX_VENDOR_LIBRARY_NAME=nvidia <app>
env = QS_UI_SCALE_MULTIPLIER,0.6
